fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
asyncpg>=0.29,<1
# psycopg2 is only used by the seed script (it's a synchronous load,
# fine for batch insert; the runtime app never imports it).
psycopg2-binary>=2.9,<3
